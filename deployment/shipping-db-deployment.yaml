---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: shipping-db
  labels:
    name: shipping-db
  namespace: default
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: shipping-db
    spec:
      containers:
      - name: shipping-db
        image: mongo

        ports:
        - name: mongo
          containerPort: 27017
        securityContext:
          capabilities:
            drop:
              - all
            add:
              - CHOWN
              - SETGID
              - SETUID
          readOnlyRootFilesystem: true
        volumeMounts:
        - mountPath: /tmp
          name: tmp-volume
      volumes:
        - name: tmp-volume
          emptyDir:
            medium: Memory
      nodeSelector:
        beta.kubernetes.io/os: linux